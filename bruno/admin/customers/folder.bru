meta {
  name: customers
  seq: 3
}

auth {
  mode: inherit
}

docs {
  Admin customer management (EPIC-002 STORY-001). Requires `adminAccessToken` and customers:read / customers:notify permissions.
}
