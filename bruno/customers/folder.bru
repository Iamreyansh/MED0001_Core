meta {
  name: customers
  seq: 22
}

auth {
  mode: inherit
}

docs {
  Customer profile self-service, address book, Namma Money wallet, loyalty/referrals, and saved payment methods (EPIC-002). Requires `accessToken` from auth/customer/verify-otp.
}
