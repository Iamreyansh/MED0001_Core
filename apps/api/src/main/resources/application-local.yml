spring:
  datasource:
    url: jdbc:h2:mem:medmate;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

logging:
  level:
    root: INFO

medmate:
  internal:
    service-token: local-internal-wallet-token
  inventory:
    growth-features-enabled: true
    jobs:
      enabled: true
    po-dispatch:
      whatsapp-enabled: true
      email-enabled: true
  pos:
    starter-features-enabled: true
    growth-features-enabled: true
    notifications-enabled: true
    jobs:
      enabled: true
  kyc:
    webhook-secret: local-kyc-webhook-secret
  razorpay:
    webhook-secret: test_razorpay_webhook_secret
  razorpayx:
    webhook-secret: local-razorpayx-webhook-secret
